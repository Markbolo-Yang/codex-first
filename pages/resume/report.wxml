<view class="page-shell">
  <view class="navigation-bar" style="height: {{navigationBarHeight}}px; padding-top: {{navigationBarHeight - navigationContentHeight}}px;">
    <view class="navigation-content" style="height: {{navigationContentHeight}}px;">
      <view class="navigation-back" bindtap="requestLeave" aria-label="返回">
        <view class="navigation-back-icon"></view>
      </view>
      <view class="navigation-title">简历诊断报告</view>
    </view>
  </view>

  <view class="container">
    <view class="card" wx:if="{{report.overview}}">
      <view class="card-title"><text>简历概况盘点</text></view>
      <view class="card-content"><text class="desc-line" selectable="true" user-select="true">{{report.overview}}</text></view>
    </view>

    <view class="card" wx:if="{{report.advantages.length}}">
      <view class="card-title"><text>简历优势分析</text></view>
      <view class="card-content">
        <text class="advantage-line" wx:for="{{report.advantages}}" wx:for-item="advantage" wx:key="index" selectable="true" user-select="true">✅ {{advantage}}</text>
      </view>
    </view>

    <view class="card" wx:if="{{report.improve.base_info.length || report.improve.projects.length}}">
      <view class="card-title"><text>待提升分析</text></view>
      <view class="sub-card" wx:if="{{report.improve.base_info.length}}">
        <view class="sub-title">基础信息待优化</view>
        <text class="improve-line" wx:for="{{report.improve.base_info}}" wx:for-item="point" wx:key="index" selectable="true" user-select="true">❓ {{point}}</text>
      </view>
      <view class="sub-card" wx:if="{{report.improve.projects.length}}">
        <view class="sub-title">项目履历待优化（重点）</view>
        <view class="project-item" wx:for="{{report.improve.projects}}" wx:for-item="project" wx:key="index">
          <text class="project-name" selectable="true" user-select="true">{{project.project_name}}</text>
          <text class="improve-line" wx:for="{{project.points}}" wx:for-item="point" wx:key="index" selectable="true" user-select="true">❓ {{point}}</text>
        </view>
      </view>
    </view>

    <view class="card" wx:if="{{report.optimize_ref.length}}">
      <view class="card-title"><text>优化方案参考</text></view>
      <view class="sub-card">
        <view class="compare-box" wx:for="{{report.optimize_ref}}" wx:for-item="reference" wx:key="index">
          <text class="project-label" selectable="true" user-select="true">{{reference.project_name || '优化建议'}}</text>
          <text class="original" wx:if="{{reference.original_text}}" selectable="true" user-select="true">原文：{{reference.original_text}}</text>
          <view class="optimize" wx:if="{{reference.optimize_content}}">
            <text selectable="true" user-select="true">{{reference.optimize_content}}</text>
            <view class="copy-btn" wx:if="{{isCompleted}}" bindtap="copyText" data-text="{{reference.optimize_content}}">一键复制</view>
          </view>
        </view>
      </view>
    </view>

    <view class="generating-indicator" wx:if="{{hasReportContent && !isCompleted}}">
      <text class="generating-dot">●</text>
      报告持续生成中...
    </view>

    <view class="bottom-btn" wx:if="{{isCompleted}}" bindtap="goToMentorTab" data-leave-target="mentor">转向真人导师咨询</view>
  </view>

  <view class="leave-mask" wx:if="{{showLeaveReminder}}" catchtouchmove="preventModalClose">
    <view class="leave-dialog" catchtap="preventModalClose">
      <view class="leave-dialog-accent"></view>
      <view class="leave-dialog-title">先别急</view>
      <view class="leave-dialog-copy">
        离开后暂不能再次查看，<text>可以先复制或截图哦</text>
      </view>
      <view class="leave-dialog-actions">
        <button class="leave-action leave-action-secondary" bindtap="continueReading">继续看看</button>
        <button class="leave-action leave-action-primary" bindtap="confirmLeave">执意离开</button>
      </view>
    </view>
  </view>
</view>
